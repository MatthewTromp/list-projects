Provides a tabulated-list-mode which displays all the projects known to emacs to exist.

You can install it with `M-x package-vc-install RET https://github.com/MatthewTromp/list-projects RET`

I suggest binding list-projects to C-x p l.
